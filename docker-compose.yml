services:
  portfolio:
    build: .
    container_name: portfolio

    ports:
      - "8081:80"

    restart: unless-stopped

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost"]
      interval: 30s
      timeout: 10s
      retries: 3
